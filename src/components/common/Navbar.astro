---

---

<header class="main-header">
  <div class="header-container">
    <div class="logo-container">
      <a href="/">
        <img src="/logos/LOGO-BLANCO.avif" alt="Logo CUH COMIIN 2026" class="logo" />
      </a>
    </div>
    
    <nav class="main-nav">
      <ul class="nav-menu">
        <li class="nav-item">
          <a href="/" class="nav-link">Inicio</a>
        </li>
        
        <li class="nav-item has-dropdown">
          <button class="nav-link dropdown-trigger" aria-expanded="false" aria-haspopup="true">
            El Congreso
            <i class="fas fa-chevron-down dropdown-icon" aria-hidden="true"></i>
          </button>
          <ul class="dropdown-menu">
            <li><a href="/about" aria-label="Ir a página ¿Qué es COMIIN?">¿Qué es COMIIN?</a></li>
            <li><a href="/mision-vision" aria-label="Ir a página Misión y Visión">Misión y Visión</a></li>
            <li><a href="/comite" aria-label="Ir a página Comité Organizador">Comité Organizador</a></li>
            <li><a href="/memorias" aria-label="Ir a página Memorias Anteriores">Memorias Anteriores</a></li>
          </ul>
        </li>
        
        <li class="nav-item has-dropdown">
          <button class="nav-link dropdown-trigger" aria-expanded="false" aria-haspopup="true">
            Participantes
            <i class="fas fa-chevron-down dropdown-icon" aria-hidden="true"></i>
          </button>
          <ul class="dropdown-menu">
            <li><a href="/conferencia-magistral" aria-label="Ir a página Conferencia Magistral">Conferencia Magistral</a></li>
            <li><a href="/talleristas" aria-label="Ir a página Talleristas">Talleristas</a></li>
            <li><a href="/convocatoria" aria-label="Ir a página Convocatoria">Convocatoria</a></li>
          </ul>
        </li>
        
        <li class="nav-item">
          <a href="/programa" class="nav-link">Programa</a>
        </li>
        
        <li class="nav-item has-dropdown">
          <button class="nav-link dropdown-trigger" aria-expanded="false" aria-haspopup="true">
            Registro
            <i class="fas fa-chevron-down dropdown-icon" aria-hidden="true"></i>
          </button>
          <ul class="dropdown-menu dropdown-right">
            <li><a href="/registro-ponente" aria-label="Ir a página Registro como Ponente">Como Ponente</a></li>
            <li><a href="/registro-asistente" aria-label="Ir a página Registro como Asistente">Como Asistente</a></li>
          </ul>
        </li>
        
        <li class="nav-item">
          <a href="/sedes" class="nav-link">Sedes</a>
        </li>
        
        <!-- Logo de fondo para móvil -->
        <li class="mobile-menu-logo" aria-hidden="true">
          <img src="/logos/marcacuh.avif" alt="" />
        </li>
      </ul>
      
      <button class="mobile-menu-toggle" aria-label="Abrir menú de navegación">
        <span class="hamburger-line"></span>
        <span class="hamburger-line"></span>
        <span class="hamburger-line"></span>
      </button>
    </nav>
  </div>
</header>

<script>
  // Función para inicializar el navbar
  function initNavbar() {
    const menuToggle = document.querySelector('.mobile-menu-toggle');
    const navMenu = document.querySelector('.nav-menu');
    const body = document.body;
    
    // Limpiar clases previas si existen
    menuToggle?.classList.remove('active');
    navMenu?.classList.remove('active');
    body.classList.remove('menu-open');
    
    // Limpiar todos los dropdowns
    const allDropdowns = document.querySelectorAll('.has-dropdown');
    allDropdowns.forEach((item: Element) => {
      item.classList.remove('dropdown-open');
      const trigger = item.querySelector('.dropdown-trigger');
      trigger?.setAttribute('aria-expanded', 'false');
    });
    
    // Remover event listeners previos clonando el botón de menú
    if (menuToggle && menuToggle.parentNode) {
      const newMenuToggle = menuToggle.cloneNode(true) as HTMLElement;
      menuToggle.parentNode.replaceChild(newMenuToggle, menuToggle);
    }
    
    // Remover event listeners previos de los dropdown triggers clonándolos
    const oldDropdownTriggers = document.querySelectorAll('.dropdown-trigger');
    oldDropdownTriggers.forEach(oldTrigger => {
      if (oldTrigger.parentNode) {
        const newTrigger = oldTrigger.cloneNode(true) as HTMLElement;
        oldTrigger.parentNode.replaceChild(newTrigger, oldTrigger);
      }
    });
    
    const updatedMenuToggle = document.querySelector('.mobile-menu-toggle');
    
    // Toggle menú móvil
    updatedMenuToggle?.addEventListener('click', (e) => {
      e.stopPropagation();
      updatedMenuToggle.classList.toggle('active');
      navMenu?.classList.toggle('active');
      body.classList.toggle('menu-open');
    });

    // SOLO MOBILE: Manejar clicks en dropdowns para abrir/cerrar
    const isMobile = window.innerWidth <= 768;
    
    if (isMobile) {
      const hasDropdownItems = document.querySelectorAll('.has-dropdown');
      const dropdownTriggers = document.querySelectorAll('.dropdown-trigger');
      
      dropdownTriggers.forEach(trigger => {
        const parentItem = trigger.closest('.has-dropdown');
        
        trigger.addEventListener('click', (e) => {
          e.preventDefault();
          e.stopPropagation();
          
          const isOpen = parentItem?.classList.contains('dropdown-open');
          
          // Cerrar otros dropdowns
          hasDropdownItems.forEach((item: Element) => {
            if (item !== parentItem) {
              item.classList.remove('dropdown-open');
              const itemTrigger = item.querySelector('.dropdown-trigger');
              itemTrigger?.setAttribute('aria-expanded', 'false');
            }
          });
          
          // Toggle el dropdown actual
          if (parentItem) {
            parentItem.classList.toggle('dropdown-open');
            trigger.setAttribute('aria-expanded', isOpen ? 'false' : 'true');
          }
        });
      });
    }

    // Cerrar menú cuando se hace click en links del dropdown
    const dropdownLinks = document.querySelectorAll('.dropdown-menu a');
    dropdownLinks.forEach(link => {
      link.addEventListener('click', () => {
        updatedMenuToggle?.classList.remove('active');
        navMenu?.classList.remove('active');
        body.classList.remove('menu-open');
        
        // Cerrar todos los dropdowns
        allDropdowns.forEach((item: Element) => {
          item.classList.remove('dropdown-open');
          const itemTrigger = item.querySelector('.dropdown-trigger');
          itemTrigger?.setAttribute('aria-expanded', 'false');
        });
      });
    });

    // Click en links sin dropdown
    const simpleLinks = document.querySelectorAll('.nav-item:not(.has-dropdown) .nav-link');
    simpleLinks.forEach(link => {
      link.addEventListener('click', () => {
        updatedMenuToggle?.classList.remove('active');
        navMenu?.classList.remove('active');
        body.classList.remove('menu-open');
      });
    });

    // Cerrar menú al hacer click en el logo
    const logoLink = document.querySelector('.logo-container a');
    logoLink?.addEventListener('click', () => {
      updatedMenuToggle?.classList.remove('active');
      navMenu?.classList.remove('active');
      body.classList.remove('menu-open');
      
      // Cerrar todos los dropdowns
      allDropdowns.forEach((item: Element) => {
        item.classList.remove('dropdown-open');
        const itemTrigger = item.querySelector('.dropdown-trigger');
        itemTrigger?.setAttribute('aria-expanded', 'false');
      });
    });

    // Cerrar menú al hacer click fuera (solo mobile)
    document.addEventListener('click', (e) => {
      const target = e.target as HTMLElement;
      if (!target.closest('.main-nav') && navMenu?.classList.contains('active')) {
        updatedMenuToggle?.classList.remove('active');
        navMenu?.classList.remove('active');
        body.classList.remove('menu-open');
        
        // Cerrar todos los dropdowns
        allDropdowns.forEach((item: Element) => {
          item.classList.remove('dropdown-open');
          const itemTrigger = item.querySelector('.dropdown-trigger');
          itemTrigger?.setAttribute('aria-expanded', 'false');
        });
      }
    });
  }
  
  // Inicializar en carga inicial
  document.addEventListener('DOMContentLoaded', initNavbar);
  
  // Reinicializar después de navegaciones de Astro (View Transitions)
  document.addEventListener('astro:page-load', initNavbar);
</script>


<!-- Paleta de colores actualizada -->
<!-- Color Navbar: #1F407F -->
<!-- Color Selector/Hover: #58C2DC -->

<style>
  @keyframes fadeInDown {
    from {
      opacity: 0;
      transform: translateY(-15px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  :root {
    --navbar-height: 95px;
  }

  .main-header {
    background: #1F407F;
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 1000;
    transition: background 0.4s cubic-bezier(0.4, 0, 0.2, 1), box-shadow 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    height: var(--navbar-height);
  }

  .main-header.scrolled {
    background: #1F407F;
    box-shadow: 0 2px 20px rgba(0, 0, 0, 0.1);
  }
  
  .header-container {
    max-width: 1400px;
    margin: 0 auto;
    padding: 0 2rem;
    display: flex;
    justify-content: space-between;
    align-items: center;
    height: var(--navbar-height);
  }

  .logo-container {
    flex-shrink: 0;
    z-index: 1001;
  }

  .logo-container a {
    display: block;
    line-height: 0;
  }

  .logo {
    height: 95px;
    width: auto;
    transition: transform 0.4s ease, filter 0.4s ease, height 0.4s ease;
    filter: drop-shadow(0 2px 8px rgba(0, 0, 0, 0.2));
  }

  .main-header.scrolled .logo {
    height: 70px;
  }

  .logo:hover {
    transform: scale(1.05);
    filter: drop-shadow(0 4px 12px rgba(255, 255, 255, 0.3));
  }

  .main-nav {
    display: flex;
    align-items: center;
  }

  .nav-menu {
    display: flex;
    list-style: none;
    gap: 0.5rem;
    align-items: center;
  }

  .mobile-menu-logo {
    display: none;
  }

  .nav-item {
    position: relative;
  }

  .nav-link {
    display: flex;
    align-items: center;
    gap: 0.4rem;
    padding: 0.75rem 1.2rem;
    color: var(--white);
    text-decoration: none;
    font-weight: 500;
    font-size: 0.95rem;
    letter-spacing: 0.3px;
    transition: var(--transition);
    border-radius: 8px;
    position: relative;
    white-space: nowrap;
    background: none;
    border: none;
    cursor: pointer;
    font-family: inherit;
  }

  .nav-link::before {
    content: '';
    position: absolute;
    bottom: 0;
    left: 50%;
    transform: translateX(-50%);
    width: 0;
    height: 2px;
    background: linear-gradient(90deg, transparent, #58C2DC, transparent);
    transition: width 0.3s ease;
  }

  .nav-link:hover::before {
    width: 80%;
  }

  .nav-link:hover {
    background: rgba(88, 194, 220, 0.1);
    color: var(--white);
    transform: translateY(-2px);
  }

  .dropdown-icon {
    font-size: 0.7rem;
    transition: transform 0.3s ease;
  }

  .dropdown-menu {
    position: absolute;
    top: calc(100% + 0.5rem);
    left: 0;
    background: linear-gradient(135deg, rgba(20, 48, 99, 0.9) 0%, rgba(15, 39, 80, 0.95) 100%);
    backdrop-filter: blur(12px);
    list-style: none;
    min-width: 240px;
    border-radius: 12px;
    box-shadow: 0 10px 40px rgba(0, 0, 0, 0.3);
    opacity: 0;
    visibility: hidden;
    transform: translateY(-10px);
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    overflow: hidden;
    border: 1px solid rgba(88, 194, 220, 0.2);
  }

  .dropdown-right {
    left: auto;
    right: 0;
  }

  .dropdown-menu li {
    transform: translateX(-20px);
    opacity: 0;
    transition: all 0.3s ease;
  }

  @media (min-width: 769px) {
    .has-dropdown:hover .dropdown-menu {
      opacity: 1;
      visibility: visible;
      transform: translateY(0);
    }
    .has-dropdown:hover .dropdown-icon {
      transform: rotate(180deg);
    }
    .has-dropdown:hover .dropdown-menu li {
      transform: translateX(0);
      opacity: 1;
    }
    .has-dropdown:hover .dropdown-menu li:nth-child(1) { transition-delay: 0.05s; }
    .has-dropdown:hover .dropdown-menu li:nth-child(2) { transition-delay: 0.1s; }
    .has-dropdown:hover .dropdown-menu li:nth-child(3) { transition-delay: 0.15s; }
    .has-dropdown:hover .dropdown-menu li:nth-child(4) { transition-delay: 0.2s; }
  }

  .dropdown-menu a {
    display: block;
    padding: 0.9rem 1.5rem;
    color: var(--white);
    text-decoration: none;
    font-size: 0.9rem;
    transition: all 0.2s ease;
    position: relative;
    overflow: hidden;
  }

  .dropdown-menu a::before {
    content: '';
    position: absolute;
    left: 0;
    top: 0;
    height: 100%;
    width: 3px;
    background: #58C2DC;
    transform: scaleY(0);
    transition: transform 0.3s ease;
  }

  .dropdown-menu a:hover::before {
    transform: scaleY(1);
  }

  .dropdown-menu a:hover {
    background: rgba(88, 194, 220, 0.15);
    padding-left: 2rem;
    color: var(--white);
  }

  .mobile-menu-toggle {
    display: none;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    gap: 5px;
    width: 36px;
    height: 36px;
    background: transparent;
    border: none;
    cursor: pointer;
    padding: 0;
    z-index: 1001;
    transition: transform 0.3s ease;
    position: relative;
  }
  
  .mobile-menu-toggle:hover {
    transform: scale(1.1);
  }

  .hamburger-line {
    width: 26px;
    height: 2.5px;
    background: var(--white);
    border-radius: 3px;
    transition: all 0.35s cubic-bezier(0.4, 0, 0.2, 1);
    transform-origin: center;
    position: absolute;
  }

  .hamburger-line:nth-child(1) {
    top: 9px;
  }

  .hamburger-line:nth-child(2) {
    top: 50%;
    transform: translateY(-50%);
  }

  .hamburger-line:nth-child(3) {
    bottom: 9px;
  }

  .mobile-menu-toggle.active .hamburger-line:nth-child(1) {
    top: 50%;
    transform: translateY(-50%) rotate(45deg);
  }

  .mobile-menu-toggle.active .hamburger-line:nth-child(2) {
    opacity: 0;
    transform: translateY(-50%) scale(0.8);
  }

  .mobile-menu-toggle.active .hamburger-line:nth-child(3) {
    bottom: 50%;
    transform: translateY(50%) rotate(-45deg);
  }
  
  @media (max-width: 1024px) {
    .nav-link {
      padding: 0.7rem 1rem;
      font-size: 0.9rem;
    }
    .main-header.scrolled .logo, .logo {
      height: 70px;
    }
  }

  @media (max-width: 768px) {
    :root {
      --navbar-height: 70px;
    }

    .main-header, .main-header.scrolled {
      height: var(--navbar-height);
    }
    
    .main-header.scrolled .logo, .logo {
      height: 60px;
    }

    .header-container {
      padding: 0 1.5rem;
    }

    .mobile-menu-toggle {
      display: flex;
    }

    .nav-menu {
      position: fixed;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: rgba(15, 39, 80, 0.9);
      backdrop-filter: blur(15px);
      -webkit-backdrop-filter: blur(15px);
      flex-direction: column;
      gap: 0;
      padding: calc(var(--navbar-height) + 2rem) 0 2rem 0;
      margin: 0;
      overflow-y: auto;
      visibility: hidden;
      opacity: 0;
      transition: opacity 0.4s cubic-bezier(0.4, 0, 0.2, 1), visibility 0.4s;
      z-index: 999;
    }

    .nav-menu.active {
      visibility: visible;
      opacity: 1;
    }

    .nav-item {
      width: 100%;
      opacity: 0;
      transform: translateY(-15px);
      transition: opacity 0.4s ease, transform 0.4s ease;
    }

    .nav-menu.active .nav-item {
      opacity: 1;
      transform: translateY(0);
    }
    
    .nav-menu.active .nav-item:nth-child(1) { transition-delay: 0.1s; }
    .nav-menu.active .nav-item:nth-child(2) { transition-delay: 0.15s; }
    .nav-menu.active .nav-item:nth-child(3) { transition-delay: 0.2s; }
    .nav-menu.active .nav-item:nth-child(4) { transition-delay: 0.25s; }
    .nav-menu.active .nav-item:nth-child(5) { transition-delay: 0.3s; }
    .nav-menu.active .nav-item:nth-child(6) { transition-delay: 0.35s; }

    .mobile-menu-logo {
      display: flex;
      align-items: center;
      justify-content: center;
      margin-top: auto;
      padding: 3rem 1.5rem 2rem 1.5rem;
      pointer-events: none;
    }

    .mobile-menu-logo img {
      width: 100%;
      max-width: 250px;
      height: auto;
      opacity: 0.1;
      filter: grayscale(80%);
    }

    .nav-link {
      width: 100%;
      justify-content: space-between;
      padding: 1.1rem 2rem;
      font-size: 1.1rem;
      font-weight: 500;
      border-radius: 0;
      border-bottom: 1px solid rgba(255, 255, 255, 0.05);
      white-space: normal;
      line-height: 1.5;
      text-align: left;
    }

    .nav-link::before {
      display: none;
    }

    .nav-link:active,
    .has-dropdown.dropdown-open > .nav-link {
      background: rgba(88, 194, 220, 0.1);
      transform: none;
      color: #58C2DC;
    }

    .dropdown-icon {
      font-size: 0.8rem;
      margin-left: auto;
      padding-left: 1rem;
    }

    .dropdown-menu {
      position: static;
      opacity: 1;
      visibility: visible;
      transform: none;
      background: rgba(0, 0, 0, 0.2);
      box-shadow: inset 0 4px 10px rgba(0,0,0,0.2);
      border-radius: 0;
      border: none;
      max-height: 0;
      overflow: hidden;
      transition: max-height 0.4s cubic-bezier(0.4, 0, 0.2, 1), padding 0.4s ease;
      padding: 0;
      margin: 0;
    }

    .has-dropdown.dropdown-open .dropdown-menu {
      max-height: 400px;
      padding: 0.5rem 0;
    }

    .has-dropdown.dropdown-open .dropdown-icon {
      transform: rotate(180deg);
    }

    .dropdown-menu li {
      transform: none;
      opacity: 1;
      transition: none;
      border-bottom: none;
    }

    .dropdown-menu a {
      padding: 0.8rem 2rem 0.8rem 3rem;
      font-size: 1rem;
      transition: all 0.2s ease;
      display: flex;
      align-items: center;
      color: rgba(255, 255, 255, 0.8);
    }

    .dropdown-menu a::before {
      content: '—';
      position: static;
      width: auto;
      height: auto;
      background: none;
      transform: none;
      margin-right: 0.75rem;
      opacity: 0.6;
      transition: all 0.3s ease;
    }

    .dropdown-menu a:active,
    .dropdown-menu a:hover {
      background: rgba(88, 194, 220, 0.1);
      color: var(--white);
      padding-left: 3.25rem;
    }

    .dropdown-menu a:hover::before {
      opacity: 1;
      color: #58C2DC;
    }

    :global(body.menu-open) {
      overflow: hidden;
    }

    .nav-menu::-webkit-scrollbar {
      display: none;
    }
    .nav-menu {
      -ms-overflow-style: none;
      scrollbar-width: none;
    }
  }

  @media (max-width: 480px) {
    :root {
      --navbar-height: 60px;
    }

    .main-header, .main-header.scrolled {
      height: var(--navbar-height);
    }
    
    .main-header.scrolled .logo, .logo {
      height: 50px;
    }

    .header-container {
      padding: 0 1rem;
    }

    .nav-link {
      padding: 1rem 1.5rem;
      font-size: 1rem;
    }

    .dropdown-menu a {
      padding: 0.75rem 1.5rem 0.75rem 2.5rem;
      font-size: 0.9rem;
    }

    .dropdown-menu a:active,
    .dropdown-menu a:hover {
      padding-left: 2.75rem;
    }
  }
</style>