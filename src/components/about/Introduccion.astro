---

---
<section class="introduccion-section">
  <div class="container">
    <header class="section-header">
      <h2>CINCISOH 2024: Un Éxito sin Precedentes</h2>
    </header>
    
    <div class="introduccion-content">
      <!-- Estadísticas (columna izquierda) -->
      <aside class="stats-grid">
        <div class="stat-card">
          <div class="stat-number" data-value="300" data-suffix="+">0</div>
          <div class="stat-label">Participantes</div>
        </div>
        <div class="stat-card">
          <div class="stat-number" data-value="80" data-suffix="+">0</div>
          <div class="stat-label">Proyectos Presentados</div>
        </div>
        <div class="stat-card">
          <div class="stat-number" data-value="6">0</div>
          <div class="stat-label">Áreas Temáticas</div>
        </div>
      </aside>

      <!-- Texto principal (columna derecha) -->
      <article class="texto-content">
        <p>
          El <strong>Congreso de Investigación en Ciencias Sociales y Humanidades (CINCISOH)</strong> 
          del Centro Universitario Hidalguense (CUH), marcó un acontecimiento significativo en el 
          <strong>fortalecimiento del compromiso institucional</strong> por la generación, difusión 
          y aplicación del conocimiento científico en el ámbito de las Ciencias Sociales y Humanidades. 
          Este evento reunió a <strong>investigadores, docentes, estudiantes de posgrado y profesionales</strong> 
          interesados por reflexionar acerca de los retos y desafíos contemporáneos que se viven día a día 
          en la sociedad, así como compartir avances, experiencias y resultados de investigación, orientados 
          a la <strong>comprensión y transformación de los fenómenos sociales</strong>.
        </p>
        
        <p>
          Durante el CINCISOH se desarrollaron <strong>Conferencias Magistrales, Conferencias, Talleres 
          y Ponencias en Mesas de Trabajo</strong> que abordaron ejes temáticos vinculados con la Educación, 
          Derecho, Administración, Desarrollo Humano, Desigualdad, Pobreza, Exclusión Social, Género, Feminismo, 
          Sociología, Políticas Educativas, Psicología Educativa, Ciencia, Innovación y Tecnologías Digitales. 
          Estas actividades dieron pauta al diálogo en el que se destacó la importancia de 
          <strong>fortalecer la investigación como pilar y motor para la innovación social</strong>.
        </p>
        
        <p>
          El congreso permitió ser un medio por el cuál vislumbrar la realidad social, en diferentes contextos 
          a través de los diferentes trabajos académicos de diferentes <strong>Instituciones de Educación Superior (IES)</strong> 
          tales como, CUH, Universidad Autónoma del Estado de Hidalgo (UAEH), Corporación Universitaria Minuto de Dios 
          de Colombia y de la Universidad Autónoma de Tamaulipas (UAT), por mencionar algunas, promoviendo la 
          <strong>colaboración y vinculación entre ellas</strong>, asegurando su participación para el siguiente congreso. 
          Asimismo, favoreció al cimiento de un camino hacia una <strong>comunidad investigadora comprometida</strong> 
          con el pensamiento crítico, ética profesional y con la búsqueda de soluciones a las problemáticas sociales 
          contemporáneas.
        </p>
        
        <p>
          El <strong>Primer Congreso de Investigación en Ciencias Sociales y Humanidades</strong>, constituyó un 
          punto de partida para el <strong>impulso continuo y permanente de la investigación</strong>, reafirmando 
          el compromiso del Centro Universitario Hidalguense de contribuir al desarrollo científico, humanístico y 
          social al crear <strong>espacios de encuentro, reflexión y construcción colectiva del conocimiento</strong>.
        </p>
      </article>
    </div>
  </div>
</section>

<script>
  function initCounters() {
    // Función de animación principal
    function animateCountUp(el: HTMLElement) {
      const target = parseInt(el.dataset.value || '0', 10);
      const suffix = el.dataset.suffix || '';
      const duration = 2000;
      let startTime: number | null = null;

      function step(timestamp: number) {
        if (!startTime) startTime = timestamp;
        
        const progress = Math.min((timestamp - startTime) / duration, 1);
        const easedProgress = 1 - Math.pow(1 - progress, 3);
        const currentNumber = Math.floor(easedProgress * target);
        
        el.textContent = String(currentNumber);
        
        if (progress < 1) {
          requestAnimationFrame(step);
        } else {
          el.textContent = target + suffix;
        }
      }
      
      requestAnimationFrame(step);
    }

    // Observador de Intersección
    const observer = new IntersectionObserver((entries, observer) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          const el = entry.target as HTMLElement;
          animateCountUp(el);
          observer.unobserve(el);
        }
      });
    }, {
      threshold: 0.5
    });

    // Observar todos los números
    const statNumbers = document.querySelectorAll('.stat-number');
    statNumbers.forEach(el => {
      observer.observe(el as HTMLElement);
    });
  }

  // Inicializar en carga inicial
  document.addEventListener('DOMContentLoaded', initCounters);
  
  // Reinicializar con View Transitions
  document.addEventListener('astro:page-load', initCounters);
</script>

<style>
  .introduccion-section {
    padding: 5rem 2rem;
    background: linear-gradient(180deg, #f8f9fa 0%, #e9ecef 100%);
  }

  .container {
    max-width: 1200px;
    margin-inline: auto;
  }

  /* Header de la sección */
  .section-header {
    text-align: center;
    margin-block-end: 3rem;
  }

  .section-header h2 {
    font-size: clamp(2rem, 5vw, 2.5rem);
    font-weight: 700;
    color: #1F407F;
    position: relative;
    display: inline-block;
    padding-block-end: 1rem;
  }
 
  .section-header h2::after {
    content: '';
    position: absolute;
    inset-block-end: 0;
    inset-inline-start: 50%;
    transform: translateX(-50%);
    width: 80px;
    height: 4px;
    background: #1F407F;
    border-radius: 2px;
  }

  /* Grid principal: Stats izquierda, Texto derecha */
  .introduccion-content {
    display: grid;
    grid-template-columns: 280px 1fr;
    gap: 3rem;
    align-items: start;
  }

  /* Grid de estadísticas (columna izquierda) */
  .stats-grid {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
    position: sticky;
    top: calc(var(--navbar-height) + 2rem);
  }

  /* Contenido de texto (columna derecha) */
  .texto-content {
    display: flex;
    flex-direction: column;
    gap: 1.25rem;
  }

  .texto-content p {
    font-size: clamp(1rem, 2vw, 1.1rem);
    color: var(--text-color);
    line-height: 1.8;
    text-wrap: pretty;
    text-align: left;
  }

  .texto-content p strong {
    color: #1F407F;
    font-weight: 600;
  }

  .stat-card {
    background: var(--white);
    border-radius: 12px;
    padding: 2.5rem 1.5rem;
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    border: 1px solid rgba(31, 64, 127, 0.08);
    text-align: center;
    position: relative;
    overflow: hidden;
  }

  .stat-card::before {
    content: '';
    position: absolute;
    inset-block-start: 0;
    inset-inline-start: 0;
    inset-inline-end: 0;
    height: 4px;
    background: #1F407F;
    transform: scaleX(0);
    transform-origin: left;
    transition: transform 0.4s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .stat-card:hover {
    transform: translateY(-8px);
    box-shadow: 0 15px 40px rgba(0, 0, 0, 0.15);
  }

  .stat-card:hover::before {
    transform: scaleX(1);
  }

  .stat-number {
    font-size: clamp(2.5rem, 5vw, 3rem);
    font-weight: 700;
    color: #1F407F;
    margin-block-end: 0.75rem;
    line-height: 1;
  }

  .stat-label {
    font-size: clamp(0.9rem, 2vw, 1rem);
    color: var(--text-light);
    text-transform: uppercase;
    letter-spacing: 1px;
    font-weight: 500;
  }

  /* Responsive */
  @media (max-width: 768px) {
    .introduccion-section {
      padding: 4rem 1.5rem;
    }

    .section-header {
      margin-block-end: 2.5rem;
    }

    .introduccion-content {
      grid-template-columns: 1fr;
      gap: 2.5rem;
    }

    .stats-grid {
      position: static;
      order: -1;
      gap: 1.25rem;
    }

    .texto-content {
      gap: 1.25rem;
    }

    .texto-content p {
      text-align: left;
    }
  }

  @media (max-width: 480px) {
    .introduccion-section {
      padding: 3rem 1rem;
    }

    .section-header h2::after {
      width: 60px;
      height: 3px;
    }

    .stats-grid {
      gap: 1rem;
    }

    .stat-card {
      padding: 1.75rem 1rem;
    }

    .texto-content {
      gap: 1rem;
    }
  }
</style>
