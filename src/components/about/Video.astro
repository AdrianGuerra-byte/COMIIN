---

---

<section class="video-section">
  <div class="container">
    <div class="video-layout">
      <!-- Contenido izquierdo: Información -->
      <aside class="video-info">
        <header class="section-header">
          <h2>
            <span class="highlight">CINCISOH 2024</span>
            <span class="subtitle">Primer Congreso de Investigación en Ciencias Sociales y Humanidades</span>
          </h2>
        </header>
        
        <div class="video-description">
          <p>
            Revive los mejores momentos del primer congreso que reunió a investigadores, 
            docentes y estudiantes en un espacio de diálogo académico e innovación social.
          </p>
          
          <ul class="video-highlights">
            <li>
              <i class="fas fa-check-circle" aria-hidden="true"></i>
              <span>Conferencias magistrales</span>
            </li>
            <li>
              <i class="fas fa-check-circle" aria-hidden="true"></i>
              <span>Talleres especializados</span>
            </li>
            <li>
              <i class="fas fa-check-circle" aria-hidden="true"></i>
              <span>Presentación de proyectos</span>
            </li>
            <li>
              <i class="fas fa-check-circle" aria-hidden="true"></i>
              <span>Networking académico</span>
            </li>
          </ul>
        </div>
      </aside>

      <!-- Contenido derecho: Video -->
      <article class="video-container">
        <div class="video-wrapper">
          <iframe
            width="560"
            height="315"
            src="https://www.youtube.com/embed/mSLuJYtl89Y?si=wifGH45YBTS_t-Rr"
            title="Video resumen CINCISOH 2024 - Primer Congreso de Investigación en Ciencias Sociales y Humanidades"
            frameborder="0"
            allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share"
            referrerpolicy="strict-origin-when-cross-origin"
            allowfullscreen
            loading="lazy"
          ></iframe>
          <div class="video-overlay"></div>
        </div>
      </article>
    </div>
  </div>
</section>

<style>
  .video-section {
    padding: 5rem 2rem;
    background: linear-gradient(180deg, #dee2e6 0%, var(--white) 100%);
    position: relative;
    overflow: hidden;
  }

  /* Patrón de fondo sutil */
  .video-section::before {
    content: '';
    position: absolute;
    inset: 0;
    background-image: 
      linear-gradient(45deg, rgba(31, 64, 127, 0.02) 25%, transparent 25%),
      linear-gradient(-45deg, rgba(31, 64, 127, 0.02) 25%, transparent 25%),
      linear-gradient(45deg, transparent 75%, rgba(31, 64, 127, 0.02) 75%),
      linear-gradient(-45deg, transparent 75%, rgba(31, 64, 127, 0.02) 75%);
    background-size: 60px 60px;
    background-position: 0 0, 0 30px, 30px -30px, -30px 0px;
    pointer-events: none;
  }

  .container {
    max-width: 1200px;
    margin-inline: auto;
    position: relative;
    z-index: 1;
  }

  /* Layout principal: 2 columnas */
  .video-layout {
    display: grid;
    grid-template-columns: 1fr 1.2fr;
    gap: 4rem;
    align-items: center;
  }

  /* Información del video (izquierda) */
  .video-info {
    display: flex;
    flex-direction: column;
    gap: 2rem;
  }

  .section-header h2 {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
    line-height: 1.2;
  }

  .highlight {
    font-size: clamp(2.5rem, 6vw, 3.5rem);
    font-weight: 800;
    color: #1F407F;
    letter-spacing: -0.02em;
  }

  .subtitle {
    font-size: clamp(1.1rem, 2.5vw, 1.35rem);
    font-weight: 600;
    color: var(--text-color);
    line-height: 1.4;
    text-wrap: pretty;
  }

  .video-description {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }

  .video-description p {
    font-size: clamp(1rem, 2vw, 1.1rem);
    color: var(--text-color);
    line-height: 1.7;
    text-wrap: pretty;
  }

  .video-highlights {
    list-style: none;
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 0.875rem;
    column-gap: 1.5rem;
  }

  .video-highlights li {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    font-size: 1rem;
    color: var(--text-color);
  }

  .video-highlights i {
    color: #2291C6;
    font-size: 1.25rem;
    flex-shrink: 0;
  }

  /* Contenedor del video (derecha) */
  .video-container {
    position: relative;
  }

  .video-wrapper {
    position: relative;
    padding-bottom: 56.25%; /* 16:9 Aspect Ratio */
    height: 0;
    border-radius: 16px;
    overflow: hidden;
    box-shadow: 0 20px 60px rgba(0, 0, 0, 0.2);
    border: 2px solid #1F407F;
    background: var(--primary-color);
  }

  .video-wrapper iframe {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    border: none;
  }

  /* Overlay decorativo */
  .video-overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(
      135deg,
      rgba(31, 64, 127, 0.1) 0%,
      rgba(34, 145, 198, 0.05) 100%
    );
    pointer-events: none;
    opacity: 0;
    transition: opacity 0.3s ease;
  }

  .video-wrapper:hover .video-overlay {
    opacity: 1;
  }

  /* Responsive */
  @media (max-width: 968px) {
    .video-layout {
      grid-template-columns: 1fr;
      gap: 3rem;
    }

    .video-info {
      text-align: center;
    }

    .section-header h2 {
      align-items: center;
    }

    .video-highlights {
      max-width: 400px;
      margin-inline: auto;
    }

    .video-highlights li {
      text-align: left;
    }
  }

  @media (max-width: 768px) {
    .video-section {
      padding: 4rem 1.5rem;
    }

    .video-layout {
      gap: 2.5rem;
    }

    .video-info {
      gap: 1.5rem;
    }

    .video-description {
      gap: 1.25rem;
    }

    .video-wrapper {
      border-radius: 12px;
    }
  }

  @media (max-width: 480px) {
    .video-section {
      padding: 3rem 1rem;
    }

    .video-layout {
      gap: 2rem;
    }

    .highlight {
      font-size: 2rem;
    }

    .subtitle {
      font-size: 1.05rem;
    }

    .video-highlights {
      gap: 0.75rem;
    }

    .video-highlights li {
      font-size: 0.95rem;
    }

    .video-highlights i {
      font-size: 1.1rem;
    }
  }
</style>
