---

---
<section class="registro-section" id="registro">
  <div class="container">
    <header class="section-header">
      <h1>Registro para Ponentes</h1>
      <p>Únete al COMIIN 2026 y comparte tu investigación con la comunidad académica</p>
    </header>
  </div>

  <div class="registro-content">
    <!-- Columna de Información -->
    <aside class="registro-info" aria-label="Información del registro">
      <!-- Sección Información Básica -->
      <article class="info-section">
        <header class="subsection-title">
          <h2>Información General</h2>
        </header>

        <div class="detail-item">
          <div class="icon-wrapper" aria-hidden="true">
            <i class="fas fa-calendar-alt"></i>
          </div>
          <div class="detail-content">
            <h3>Fechas Importantes</h3>
            <div class="highlight-box">
              <dl>
                <div class="date-row">
                  <dt>Apertura:</dt>
                  <dd>1 septiembre 2025</dd>
                </div>
                <div class="date-row">
                  <dt>Envío de propuestas:</dt>
                  <dd>31 diciembre 2025</dd>
                </div>
                <div class="date-row">
                  <dt>Dictamen de aceptación:</dt>
                  <dd>16 febrero 2026</dd>
                </div>
                <div class="date-row">
                  <dt>Entrega de extensos:</dt>
                  <dd>1 abril 2026</dd>
                </div>
                <div class="date-row">
                  <dt>Programa final:</dt>
                  <dd>20 abril 2026</dd>
                </div>
              </dl>
            </div>
          </div>
        </div>

        <div class="detail-item">
          <div class="icon-wrapper" aria-hidden="true">
            <i class="fas fa-money-bill-wave"></i>
          </div>
          <div class="detail-content">
            <h3>Cuota de Inscripción</h3>
            <p class="price">$400.00 <abbr title="Pesos mexicanos">MXN</abbr></p>
            <p class="price-details">Incluye: constancia, kit y acceso a todas las actividades</p>
          </div>
        </div>

        <div class="detail-item">
          <div class="icon-wrapper" aria-hidden="true">
            <i class="fas fa-file-alt"></i>
          </div>
          <div class="detail-content">
            <h3>Límite de Propuestas</h3>
            <p>Se recibirán hasta <strong>3 propuestas</strong> del mismo autor</p>
          </div>
        </div>
      </article>

      <!-- Sección Modalidades -->
      <article class="modalidades-section">
        <header class="subsection-title">
          <h2>Modalidades de Participación</h2>
        </header>

        <div class="modalidades-grid">
          <div class="modalidad-item">
            <i class="fas fa-microphone" aria-hidden="true"></i>
            <h3>Ponencias</h3>
            <p>Trabajos de investigación, estudios de caso, avances de proyectos</p>
          </div>

          <div class="modalidad-item">
            <i class="fas fa-chalkboard" aria-hidden="true"></i>
            <h3>Carteles Científicos</h3>
            <p>Presentación gráfica de proyectos, avances o resultados</p>
          </div>

          <div class="modalidad-item">
            <i class="fas fa-users" aria-hidden="true"></i>
            <h3>Talleres</h3>
            <p>Espacios de formación práctica e interacción</p>
          </div>

          <div class="modalidad-item">
            <i class="fas fa-book" aria-hidden="true"></i>
            <h3>Presentación de Libros</h3>
            <p>Difusión de obras publicadas recientemente</p>
          </div>

          <div class="modalidad-item">
            <i class="fas fa-graduation-cap" aria-hidden="true"></i>
            <h3>Encuentro de Cuerpos Académicos</h3>
            <p>Investigación y descripción de proyectos vigentes</p>
          </div>
        </div>
      </article>

      <!-- Sección Requisitos -->
      <article class="requisitos-section">
        <header class="subsection-title">
          <h2>Requisitos para el Registro</h2>
        </header>

        <ul class="requisitos-list">
          <li class="requisito-item">
            <i class="fas fa-check-circle" aria-hidden="true"></i>
            <div>
              <strong>Resumen de la propuesta</strong>
              <p>(máximo 450 palabras) que incluya: título, autor(es), institución, correo, palabras clave, objetivo, metodología, resultados y conclusiones</p>
            </div>
          </li>

          <li class="requisito-item">
            <i class="fas fa-check-circle" aria-hidden="true"></i>
            <div>
              <strong>Indicar la modalidad de participación</strong>
              <p>en la que se inscribe la propuesta</p>
            </div>
          </li>

          <li class="requisito-item">
            <i class="fas fa-check-circle" aria-hidden="true"></i>
            <div>
              <strong>Cada autor podrá registrar hasta 3 propuestas</strong>
            </div>
          </li>

          <li class="requisito-item">
            <i class="fas fa-check-circle" aria-hidden="true"></i>
            <div>
              <strong>Los trabajos serán dictaminados</strong>
              <p>por el Comité Científico del Congreso</p>
            </div>
          </li>
        </ul>
      </article>
    </aside>

    <!-- Columna del Formulario -->
    <section class="form-section" aria-labelledby="form-title">
      <header class="subsection-title">
        <h2 id="form-title">Formulario de Registro</h2>
        <p>Completa tu registro como ponente en el siguiente formulario</p>
      </header>

      <div class="form-actions">
        <a
          href="https://forms.gle/dsbAkZYZbwtHjmXi7"
          target="_blank"
          rel="noopener noreferrer"
          class="btn-primary"
        >
          <i class="fas fa-edit" aria-hidden="true"></i>
          <span>Ir al Formulario de Registro</span>
          <i class="fas fa-external-link-alt" aria-hidden="true"></i>
        </a>

        <p class="form-notice">
          <i class="fas fa-info-circle" aria-hidden="true"></i>
          Serás redirigido al formulario oficial de Google Forms para completar tu registro.
        </p>

        <a
          href="https://docs.google.com/document/d/1Elxdaar4g2SqJ6Zuu37faLls3l8554ae/export?format=docx"
          download="PropuestaPonente.docx"
          class="btn-secondary"
        >
          <i class="fas fa-download" aria-hidden="true"></i>
          <span>Descargar Plantilla de Ponente</span>
        </a>
      </div>

      <aside class="info-box" aria-labelledby="memorias-title">
        <div class="info-box-header">
          <i class="fas fa-book-open" aria-hidden="true"></i>
          <h3 id="memorias-title">Publicación de Memorias</h3>
        </div>
        <p>
          Los trabajos aceptados que envíen sus extensos serán evaluados por el Comité Científico de la <strong>Revista Codex Sapientia</strong> y, de cumplir con los requisitos, serán publicados.
        </p>
      </aside>
    </section>
  </div>
</section>

<style>
  /* ===== SECCIÓN REGISTRO PRINCIPAL ===== */
  .registro-section {
    padding: clamp(3.5rem, 8vw, 5rem) clamp(1rem, 4vw, 2rem);
    background: linear-gradient(180deg, #f8f9fa 0%, #e9ecef 100%);
    min-height: 100vh;
  }

  .registro-section .container {
    max-width: 1200px;
    margin: 0 auto;
  }

  /* Header principal */
  .section-header {
    text-align: center;
    margin-block-end: clamp(2.5rem, 6vw, 3.5rem);
  }

  .section-header h1 {
    font-size: clamp(1.85rem, 5vw, 2.5rem);
    font-weight: 700;
    color: var(--primary-color);
    margin-block-end: 0.75rem;
    position: relative;
    display: inline-block;
    padding-block-end: 0.85rem;
  }

  .section-header h1::after {
    content: "";
    position: absolute;
    inset-block-end: 0;
    inset-inline-start: 50%;
    transform: translateX(-50%);
    width: 80px;
    height: 4px;
    background: linear-gradient(90deg, var(--primary-color), var(--secondary-color));
    border-radius: 2px;
  }

  .section-header p {
    color: var(--text-light);
    font-size: clamp(0.95rem, 2.2vw, 1.05rem);
    margin-block-start: 1rem;
    max-width: 700px;
    margin-inline: auto;
    line-height: 1.6;
  }

  /* ===== LAYOUT PRINCIPAL ===== */
  .registro-content {
    display: grid;
    grid-template-columns: 1.2fr 1fr;
    gap: clamp(2rem, 4vw, 3rem);
    align-items: start;
    max-width: 1200px;
    margin: 0 auto;
  }

  .registro-info {
    display: flex;
    flex-direction: column;
    gap: clamp(1.5rem, 3vw, 2rem);
  }

  /* ===== SECCIONES DE INFORMACIÓN ===== */
  .info-section,
  .modalidades-section,
  .requisitos-section {
    background: var(--white);
    padding: clamp(1.75rem, 4vw, 2.5rem);
    border-radius: 16px;
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
    border-top: 4px solid var(--primary-color);
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .info-section:hover,
  .modalidades-section:hover,
  .requisitos-section:hover {
    box-shadow: 0 8px 30px rgba(1, 51, 103, 0.15);
    transform: translateY(-4px);
  }

  .subsection-title {
    margin-block-end: clamp(1.5rem, 3vw, 2rem);
  }

  .subsection-title h2 {
    font-size: clamp(1.35rem, 3.5vw, 1.6rem);
    color: var(--primary-color);
    font-weight: 700;
    margin-block-end: 0.5rem;
  }

  .subsection-title p {
    color: var(--text-light);
    font-size: clamp(0.9rem, 2vw, 0.95rem);
    line-height: 1.5;
  }

  /* ===== DETAIL ITEMS ===== */
  .detail-item {
    display: flex;
    align-items: flex-start;
    gap: 1.25rem;
    margin-block-end: 1.25rem;
    padding: clamp(1.25rem, 3vw, 1.5rem);
    background: linear-gradient(135deg, rgba(1, 51, 103, 0.02), rgba(0, 86, 179, 0.03));
    border-radius: 12px;
    border: 1px solid rgba(1, 51, 103, 0.08);
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .detail-item:last-child {
    margin-block-end: 0;
  }

  .detail-item:hover {
    transform: translateX(8px);
    box-shadow: 0 6px 20px rgba(1, 51, 103, 0.12);
    border-color: rgba(1, 51, 103, 0.15);
  }

  .icon-wrapper {
    flex-shrink: 0;
    width: 48px;
    height: 48px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
    color: var(--white);
    border-radius: 12px;
    font-size: 1.25rem;
    box-shadow: 0 4px 12px rgba(1, 51, 103, 0.2);
  }

  .detail-content h3 {
    margin-block-end: 0.75rem;
    color: var(--primary-color);
    font-weight: 600;
    font-size: clamp(1rem, 2.2vw, 1.1rem);
  }

  .detail-content p {
    color: var(--text-color);
    margin: 0;
    line-height: 1.6;
    font-size: clamp(0.9rem, 2vw, 0.95rem);
  }

  .price {
    font-size: clamp(1.5rem, 3vw, 1.75rem) !important;
    font-weight: 700 !important;
    color: var(--secondary-color) !important;
    margin-block-end: 0.5rem !important;
  }

  .price-details {
    font-size: 0.9rem !important;
    color: var(--text-light) !important;
  }

  /* Highlight box mejorado */
  .highlight-box {
    background: var(--white);
    padding: clamp(1rem, 2.5vw, 1.25rem);
    border-radius: 10px;
    border-inline-start: 4px solid var(--secondary-color);
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.05);
  }

  .highlight-box dl {
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: 0.65rem;
  }

  .date-row {
    display: grid;
    grid-template-columns: auto 1fr;
    gap: 0.75rem;
    align-items: baseline;
  }

  .date-row dt {
    font-weight: 600;
    color: var(--primary-color);
    font-size: clamp(0.85rem, 1.8vw, 0.9rem);
  }

  .date-row dd {
    margin: 0;
    color: var(--text-color);
    font-size: clamp(0.85rem, 1.8vw, 0.9rem);
  }

  /* ===== MODALIDADES ===== */
  .modalidades-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(min(200px, 100%), 1fr));
    gap: clamp(1rem, 2.5vw, 1.5rem);
  }

  .modalidad-item {
    text-align: center;
    padding: clamp(1.5rem, 3vw, 2rem) clamp(1rem, 2vw, 1.25rem);
    background: linear-gradient(135deg, rgba(1, 51, 103, 0.02), rgba(0, 86, 179, 0.03));
    border-radius: 12px;
    border: 2px solid rgba(1, 51, 103, 0.08);
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    position: relative;
    overflow: hidden;
  }

  .modalidad-item::before {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 4px;
    background: linear-gradient(90deg, var(--primary-color), var(--secondary-color));
    transform: scaleX(0);
    transform-origin: left;
    transition: transform 0.4s ease;
  }

  .modalidad-item:hover {
    transform: translateY(-8px);
    box-shadow: 0 12px 30px rgba(1, 51, 103, 0.18);
    border-color: var(--primary-color);
  }

  .modalidad-item:hover::before {
    transform: scaleX(1);
  }

  .modalidad-item i {
    font-size: clamp(2rem, 4vw, 2.5rem);
    color: var(--white);
    margin-block-end: 1rem;
    width: clamp(60px, 12vw, 70px);
    height: clamp(60px, 12vw, 70px);
    display: inline-flex;
    align-items: center;
    justify-content: center;
    background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
    border-radius: 50%;
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    box-shadow: 0 4px 15px rgba(1, 51, 103, 0.25);
  }

  .modalidad-item:hover i {
    transform: scale(1.1) rotate(5deg);
    box-shadow: 0 6px 20px rgba(1, 51, 103, 0.35);
  }

  .modalidad-item h3 {
    color: var(--primary-color);
    margin-block-end: 0.65rem;
    font-weight: 600;
    font-size: clamp(1rem, 2.2vw, 1.1rem);
  }

  .modalidad-item p {
    color: var(--text-light);
    margin: 0;
    font-size: clamp(0.85rem, 1.8vw, 0.9rem);
    line-height: 1.5;
  }

  /* ===== REQUISITOS ===== */
  .requisitos-list {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .requisito-item {
    display: flex;
    align-items: flex-start;
    gap: 1rem;
    padding: clamp(1rem, 2.5vw, 1.25rem);
    background: linear-gradient(135deg, rgba(1, 51, 103, 0.02), rgba(0, 86, 179, 0.03));
    border-radius: 10px;
    border-inline-start: 3px solid var(--secondary-color);
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .requisito-item:hover {
    transform: translateX(6px);
    box-shadow: 0 4px 15px rgba(1, 51, 103, 0.1);
    background: linear-gradient(135deg, rgba(1, 51, 103, 0.04), rgba(0, 86, 179, 0.05));
  }

  .requisito-item i {
    color: var(--secondary-color);
    font-size: 1.25rem;
    flex-shrink: 0;
    margin-block-start: 0.15rem;
  }

  .requisito-item div {
    flex: 1;
  }

  .requisito-item strong {
    color: var(--primary-color);
    display: block;
    margin-block-end: 0.35rem;
    font-size: clamp(0.95rem, 2vw, 1rem);
  }

  .requisito-item p {
    color: var(--text-color);
    margin: 0;
    line-height: 1.6;
    font-size: clamp(0.85rem, 1.8vw, 0.9rem);
  }

  /* ===== SECCIÓN FORMULARIO ===== */
  .form-section {
    background: var(--white);
    padding: clamp(2rem, 4vw, 2.5rem);
    border-radius: 16px;
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
    height: fit-content;
    position: sticky;
    top: calc(var(--navbar-height, 80px) + 2rem);
    border-top: 4px solid var(--secondary-color);
  }

  .form-section .subsection-title {
    text-align: center;
    margin-block-end: clamp(1.5rem, 3vw, 2rem);
  }

  .form-actions {
    display: flex;
    flex-direction: column;
    gap: 1.25rem;
  }

  /* Botones mejorados */
  .btn-primary {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 0.75rem;
    background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
    color: var(--white);
    padding: clamp(0.85rem, 2vw, 1rem) clamp(1.5rem, 3vw, 2rem);
    border-radius: 50px;
    text-decoration: none;
    font-weight: 600;
    font-size: clamp(0.95rem, 2vw, 1.05rem);
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    box-shadow: 0 4px 15px rgba(1, 51, 103, 0.3);
    border: 2px solid transparent;
    position: relative;
    overflow: hidden;
  }

  .btn-primary::before {
    content: "";
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
    transition: left 0.5s ease;
  }

  .btn-primary:hover {
    transform: translateY(-3px);
    box-shadow: 0 8px 25px rgba(1, 51, 103, 0.4);
  }

  .btn-primary:hover::before {
    left: 100%;
  }

  .btn-primary i:last-child {
    font-size: 0.85em;
    transition: transform 0.3s ease;
  }

  .btn-primary:hover i:last-child {
    transform: translateX(4px);
  }

  .btn-secondary {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 0.75rem;
    background: var(--white);
    color: var(--primary-color);
    padding: clamp(0.75rem, 1.8vw, 0.9rem) clamp(1.25rem, 2.5vw, 1.75rem);
    border-radius: 50px;
    text-decoration: none;
    font-weight: 600;
    font-size: clamp(0.9rem, 1.8vw, 1rem);
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    border: 2px solid var(--primary-color);
  }

  .btn-secondary:hover {
    background: var(--primary-color);
    color: var(--white);
    transform: translateY(-2px);
    box-shadow: 0 6px 20px rgba(1, 51, 103, 0.25);
  }

  .btn-secondary i {
    transition: transform 0.3s ease;
  }

  .btn-secondary:hover i {
    transform: scale(1.1);
  }

  .form-notice {
    text-align: center;
    color: var(--text-light);
    font-size: clamp(0.85rem, 1.8vw, 0.9rem);
    line-height: 1.6;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    padding: 0.75rem 1rem;
    background: rgba(1, 51, 103, 0.04);
    border-radius: 8px;
    margin: 0;
  }

  .form-notice i {
    color: var(--secondary-color);
    flex-shrink: 0;
  }

  /* Info box mejorado */
  .info-box {
    margin-block-start: clamp(1.5rem, 3vw, 2rem);
    padding: clamp(1.25rem, 2.5vw, 1.5rem);
    background: linear-gradient(135deg, rgba(1, 51, 103, 0.04), rgba(0, 86, 179, 0.06));
    border-radius: 12px;
    border-inline-start: 4px solid var(--secondary-color);
  }

  .info-box-header {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    margin-block-end: 0.75rem;
  }

  .info-box-header i {
    color: var(--secondary-color);
    font-size: 1.25rem;
  }

  .info-box-header h3 {
    color: var(--primary-color);
    font-size: clamp(1rem, 2.2vw, 1.1rem);
    font-weight: 600;
    margin: 0;
  }

  .info-box p {
    color: var(--text-color);
    line-height: 1.6;
    margin: 0;
    font-size: clamp(0.9rem, 1.8vw, 0.95rem);
  }

  .info-box strong {
    color: var(--primary-color);
  }

  /* ===== ANIMACIONES ===== */
  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(30px) scale(0.98);
    }
    to {
      opacity: 1;
      transform: translateY(0) scale(1);
    }
  }

  .info-section,
  .modalidades-section,
  .requisitos-section,
  .form-section {
    animation: fadeInUp 0.6s cubic-bezier(0.4, 0, 0.2, 1) backwards;
  }

  .info-section {
    animation-delay: 0.1s;
  }

  .modalidades-section {
    animation-delay: 0.2s;
  }

  .requisitos-section {
    animation-delay: 0.3s;
  }

  .form-section {
    animation-delay: 0.4s;
  }

  /* ===== RESPONSIVE DESIGN ===== */
  @media (max-width: 1024px) {
    .registro-content {
      grid-template-columns: 1fr;
      gap: clamp(1.5rem, 3vw, 2rem);
    }

    .form-section {
      position: static;
      order: -1;
    }
  }

  @media (max-width: 768px) {
    .registro-section {
      padding: clamp(3rem, 6vw, 4rem) 1.5rem;
    }

    .modalidades-grid {
      grid-template-columns: repeat(2, 1fr);
    }

    .detail-item {
      flex-direction: column;
      text-align: center;
      align-items: center;
    }

    .requisito-item {
      flex-direction: row;
      text-align: left;
    }
  }

  @media (max-width: 480px) {
    .modalidades-grid {
      grid-template-columns: 1fr;
    }

    .date-row {
      grid-template-columns: 1fr;
      gap: 0.25rem;
    }

    .date-row dt {
      font-weight: 700;
    }
  }

  /* ===== ACCESIBILIDAD ===== */
  @media (prefers-reduced-motion: reduce) {
    *,
    *::before,
    *::after {
      animation-duration: 0.01ms !important;
      animation-iteration-count: 1 !important;
      transition-duration: 0.01ms !important;
    }
  }

  /* Focus visible para accesibilidad */
  .btn-primary:focus-visible,
  .btn-secondary:focus-visible {
    outline: 3px solid var(--secondary-color);
    outline-offset: 3px;
  }
</style>
